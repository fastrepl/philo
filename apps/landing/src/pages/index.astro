---
import { getCollection, } from "astro:content";

const capabilities = [
  {
    title: "Daily notes in one timeline",
    description: "Review older notes in a continuous feed instead of bouncing through tabs and old files.",
  },
  {
    title: "Unfinished tasks roll forward",
    description: "If a task is still open when the date changes, Philo brings it into today automatically.",
  },
  {
    title: "Recurring work comes back on its own",
    description: "Daily and scheduled tasks can return automatically, so you do not have to rebuild the same plan every morning.",
  },
  {
    title: "Markdown, not lock-in",
    description: "Keep your journal as plain files on disk and use your existing Obsidian vault if you want.",
  },
  {
    title: "Free and open source",
    description: "Philo is meant to augment your vault, not turn your daily notes into another subscription silo.",
  },
];

const explainerScenes = [
  {
    label: "Timeline",
    title: "See old notes as one continuous planning surface",
    detail: "Scroll across days instead of opening and closing files.",
  },
  {
    label: "Rollover",
    title: "Open tasks move into today when the date changes",
    detail: "Yesterday closes cleanly without losing unfinished work.",
  },
  {
    label: "Recurring",
    title: "Scheduled work returns automatically",
    detail: "Repeating chores and routines show up again on their own.",
  },
  {
    label: "Markdown",
    title: "Your files stay plain text on disk",
    detail: "Use your existing vault and keep the note format portable.",
  },
  {
    label: "Open source",
    title: "The app supports your workflow instead of owning it",
    detail: "Free, inspectable, and meant to live beside the rest of your tools.",
  },
];

const examples = [
  {
    title: "Habit tracker",
    summary: "Track streaks and daily completion without leaving your note.",
    imagePath: "/examples/habit-tracker.png",
  },
  {
    title: "World clock",
    summary: "See your team time zones while planning tomorrow.",
    imagePath: "/examples/world-clock.png",
  },
  {
    title: "Calorie counter",
    summary: "Quick meal log with totals inline in the journal.",
    imagePath: "/examples/calorie-counter.png",
  },
  {
    title: "Countdown board",
    summary: "Keep launches and deadlines visible at a glance.",
    imagePath: "/examples/countdown-board.png",
  },
  {
    title: "Mood chart",
    summary: "Simple emotional trendline over the week.",
    imagePath: "/examples/mood-chart.png",
  },
  {
    title: "Meeting prep checklist",
    summary: "Turn rough notes into ready talking points.",
    imagePath: "/examples/meeting-prep-checklist.png",
  },
];

const posts = await getCollection("blog");
posts.sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Philo | Daily notes that carry unfinished work forward</title>
    <meta
      name="description"
      content="Open source, local-first daily notes app that keeps markdown in your Obsidian vault and rolls unfinished tasks into today automatically."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="ambient"></div>
    <div class="page-layout">
      <main class="shell">
        <header class="enter" style="--delay: 0ms;">
          <div class="brand-row">
            <p class="brand">Philo <span class="brand-beta">beta</span></p>
          </div>
        </header>

        <section class="hero enter" style="--delay: 70ms;">
          <h1>Daily notes that carry unfinished work forward.</h1>
          <p class="lede">
            Philo started with one problem: daily notes are a great place to think, but unfinished
            tasks get buried in yesterday&apos;s file. It keeps your journal as markdown in your
            Obsidian vault, shows notes in a timeline, and rolls open tasks into today
            automatically.
          </p>
          <p class="hero-note">
            It is built first for people who already live in daily notes and want a calmer planning
            loop, not a bigger system to manage.
          </p>
          <div class="download-split" data-download>
            <a class="download-button" data-download-primary href="https://github.com/ComputelessComputer/philo/releases/latest">
              Download for macOS
            </a><button type="button" class="download-chevron" data-download-toggle aria-label="Other platforms"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg></button>
            <div class="download-menu" data-download-menu>
              <a data-download-macos href="https://github.com/ComputelessComputer/philo/releases/latest">macOS</a>
              <a data-download-windows href="https://github.com/ComputelessComputer/philo/releases/latest">Windows</a>
              <a data-download-linux href="https://github.com/ComputelessComputer/philo/releases/latest">Linux</a>
            </div>
          </div>
          <div class="video-mobile">
            <div class="video-frame">
              <video
                class="product-video"
                controls
                muted
                loop
                playsinline
                preload="metadata"
                poster="/philo-video-poster.svg"
              >
                <source src="/philo-demo.mp4" type="video/mp4" />
              </video>
            </div>
          </div>
        </section>

        <section class="panel enter" style="--delay: 140ms;">
          <h2>Why Philo exists</h2>
          <div class="why-layout">
            <div class="why-demo" data-why-demo aria-label="Animated overview of how Philo works" role="img">
              <div class="why-demo-header">
                <span>Generated explainer</span>
                <span>5 scenes / 20 seconds</span>
              </div>
              <div class="why-demo-screen">
                {
                  explainerScenes.map((scene, index,) => (
                    <article class={`why-scene ${index === 0 ? "is-active" : ""}`} data-why-scene>
                      <p class="why-scene-label">{scene.label}</p>
                      <p class="why-scene-title">{scene.title}</p>
                      <p class="why-scene-detail">{scene.detail}</p>
                      {
                        index === 0 && (
                          <div class="scene-shell scene-timeline">
                            <div class="scene-sidebar">
                              <span>Mar 15</span>
                              <span>Mar 16</span>
                              <span class="is-current">Mar 17</span>
                            </div>
                            <div class="scene-notes">
                              <div class="scene-note-card">
                                <p>Mon 15</p>
                                <span>capture meeting notes</span>
                                <span>draft launch copy</span>
                              </div>
                              <div class="scene-note-card">
                                <p>Tue 16</p>
                                <span>plan Q2 roadmap</span>
                              </div>
                              <div class="scene-note-card is-current">
                                <p>Today</p>
                                <span>review older notes inline</span>
                                <span class="is-accent">focus on what is next</span>
                              </div>
                            </div>
                          </div>
                        )
                      }
                      {
                        index === 1 && (
                          <div class="scene-shell scene-rollover">
                            <div class="scene-column">
                              <p>Yesterday</p>
                              <span class="task-chip is-done">write project brief</span>
                              <span class="task-chip">follow up with design</span>
                            </div>
                            <div class="scene-transfer">→ rolls forward</div>
                            <div class="scene-column is-target">
                              <p>Today</p>
                              <span class="task-chip is-highlight">follow up with design</span>
                              <span class="task-chip">ship bug fix</span>
                            </div>
                          </div>
                        )
                      }
                      {
                        index === 2 && (
                          <div class="scene-shell scene-recurring">
                            <div class="scene-recurring-row">
                              <span>Daily</span>
                              <span class="task-chip is-highlight">morning review</span>
                            </div>
                            <div class="scene-recurring-row">
                              <span>Weekly</span>
                              <span class="task-chip">clean inbox</span>
                            </div>
                            <div class="scene-recurring-row">
                              <span>Monthly</span>
                              <span class="task-chip">close books</span>
                            </div>
                            <div class="scene-repeat-badge">returns on schedule</div>
                          </div>
                        )
                      }
                      {
                        index === 3 && (
                          <div class="scene-shell scene-markdown">
                            <div class="scene-tree">
                              <span>vault/</span>
                              <span>daily/</span>
                              <span class="is-current">2026-03-17.md</span>
                              <span>projects/</span>
                            </div>
                            <div class="scene-editor">
                              <p># 2026-03-17</p>
                              <span>- [ ] follow up with design</span>
                              <span>- [x] morning review</span>
                              <span>[[Roadmap]] [[Weekly Review]]</span>
                            </div>
                          </div>
                        )
                      }
                      {
                        index === 4 && (
                          <div class="scene-shell scene-open-source">
                            <div class="scene-repo-card">
                              <p>ComputelessComputer / philo</p>
                              <div class="scene-stat-row">
                                <span>open source</span>
                                <span>markdown-first</span>
                              </div>
                              <div class="scene-stat-row">
                                <span>local files stay yours</span>
                                <span>free forever</span>
                              </div>
                            </div>
                          </div>
                        )
                      }
                    </article>
                  ))
                }
                <div class="why-scene-dots" aria-hidden="true">
                  {
                    explainerScenes.map((scene, index,) => (
                      <span class={`why-scene-dot ${index === 0 ? "is-active" : ""}`} data-why-dot title={scene.label}>
                        <span class="why-scene-dot-fill" data-why-fill></span>
                      </span>
                    ))
                  }
                </div>
              </div>
            </div>
            <ul class="detail-list">
              {
                capabilities.map((item,) => (
                  <li class="detail-item">
                    <div class="detail-copy">
                      <p class="item-title">{item.title}</p>
                      <p class="item-copy">{item.description}</p>
                    </div>
                  </li>
                ))
              }
            </ul>
          </div>
        </section>

        <section class="panel enter" style="--delay: 210ms;">
          <h2>Extra utilities, if you want them</h2>
          <p class="item-copy">
            The core product is still the daily note. Widgets, AI help, and computer-use workflows
            are add-ons for the moments when plain text is not enough.
          </p>
          <div class="carousel" data-carousel>
            <div class="carousel-controls">
              <button type="button" class="carousel-button" data-carousel-prev aria-label="Previous example">←</button>
              <button type="button" class="carousel-button" data-carousel-next aria-label="Next example">→</button>
            </div>
            <div class="carousel-track" data-carousel-track>
              {
                examples.map((item,) => (
                  <article class="example-card">
                    <p class="example-title">{item.title}</p>
                    <p class="example-summary">{item.summary}</p>
                    <div class="example-preview" role="img" aria-label={`${item.title} image placeholder`}>
                      <p class="example-image-placeholder">Add image later</p>
                      <p class="example-image-path">{item.imagePath}</p>
                    </div>
                  </article>
                ))
              }
            </div>
            <div class="carousel-dots">
              {
                examples.map((item, index,) => (
                  <button
                    type="button"
                    class={`carousel-dot ${index === 0 ? "is-active" : ""}`}
                    data-carousel-dot
                    data-index={index}
                    aria-label={`Go to ${item.title}`}
                    aria-current={index === 0 ? "true" : "false"}
                  ><span class="carousel-dot-fill"></span></button>
                ))
              }
            </div>
          </div>
        </section>

        <section class="panel enter" style="--delay: 280ms;">
          <h2>Notes from the team</h2>
          <ul class="link-list">
            {
              posts.map((post,) => (
                <li>
                  <a href={`/blog/${post.slug}`}>
                    {post.data.title}
                    <span>→</span>
                  </a>
                </li>
              ))
            }
          </ul>
        </section>

        <section class="panel enter" style="--delay: 350ms;">
          <h2>Built to sit beside your vault, not replace it</h2>
          <p class="item-copy">
            Philo is open source and free forever. Your notes stay as markdown files on disk, and
            the app is meant to make daily notes lighter, not trap them in a new format or workflow.
          </p>
        </section>

        <footer class="enter" style="--delay: 420ms;">
          <nav>
            <a href="https://github.com/ComputelessComputer/philo">GitHub</a>
            <a href="/blog">Blog</a>
            <a href="/privacy">Privacy</a>
            <a href="/terms">Terms</a>
            <a href="mailto:john@hyprnote.com">Email</a>
          </nav>
        </footer>
      </main>

      <aside class="video-column enter" style="--delay: 120ms;">
        <div class="video-frame">
          <video
            class="product-video"
            controls
            muted
            loop
            playsinline
            preload="metadata"
            poster="/philo-video-poster.svg"
          >
            <source src="/philo-demo.mp4" type="video/mp4" />
          </video>
        </div>
        <p class="video-note">Place `philo-demo.mp4` in `apps/landing/public/` to use your own demo.</p>
      </aside>
    </div>
  </body>
</html>

<script is:inline>
  (async () => {
    const REPO = "ComputelessComputer/philo";
    const FALLBACK = `https://github.com/${REPO}/releases/latest`;
    const primary = document.querySelector("[data-download-primary]",);
    const toggle = document.querySelector("[data-download-toggle]",);
    const menu = document.querySelector("[data-download-menu]",);
    const macLink = document.querySelector("[data-download-macos]",);
    const winLink = document.querySelector("[data-download-windows]",);
    const linuxLink = document.querySelector("[data-download-linux]",);

    toggle?.addEventListener("click", (e,) => {
      e.stopPropagation();
      menu?.classList.toggle("is-open",);
    },);

    document.addEventListener("click", () => {
      menu?.classList.remove("is-open",);
    },);

    try {
      const res = await fetch(`https://api.github.com/repos/${REPO}/releases/latest`,);
      if (!res.ok) throw new Error();
      const data = await res.json();
      const assets = data.assets ?? [];

      const find = (/** @type {RegExp} */ re,) => assets.find((a,) => re.test(a.name,),)?.browser_download_url;

      const urls = {
        macos: find(/\.dmg$/,) ?? FALLBACK,
        windows: find(/\.exe$/,) ?? FALLBACK,
        linux: find(/\.AppImage$/,) ?? FALLBACK,
      };

      if (macLink) macLink.href = urls.macos;
      if (winLink) winLink.href = urls.windows;
      if (linuxLink) linuxLink.href = urls.linux;

      const ua = navigator.userAgent.toLowerCase();
      let os = "macos";
      if (ua.includes("win",)) os = "windows";
      else if (ua.includes("linux",)) os = "linux";

      const labels = { macos: "Download for macOS", windows: "Download for Windows", linux: "Download for Linux" };
      if (primary) {
        primary.href = urls[os];
primary.firstChild.textContent = labels[os];
      }

      const currentLink = { macos: macLink, windows: winLink, linux: linuxLink }[os];
      if (currentLink) currentLink.classList.add("is-current",);
    } catch {
      // keep fallback links
    }
  })();

  const carousel = document.querySelector("[data-carousel]",);
  if (carousel) {
    const track = carousel.querySelector("[data-carousel-track]",);
    const prev = carousel.querySelector("[data-carousel-prev]",);
    const next = carousel.querySelector("[data-carousel-next]",);
    const dots = Array.from(carousel.querySelectorAll("[data-carousel-dot]",),);
    const total = track?.children.length ?? 0;
    const DURATION = 5000;
    let ticking = false;
    let timer = null;

    const getIndex = () => {
      if (!track) {
        return 0;
      }
      return Math.round(track.scrollLeft / track.clientWidth,);
    };

    const goTo = (index,) => {
      if (!track || total === 0) {
        return;
      }
      const nextIndex = (index + total) % total;
      track.scrollTo({
        left: nextIndex * track.clientWidth,
        behavior: "smooth",
      },);
    };

    const restartTimer = () => {
      clearTimeout(timer,);
      for (const dot of dots) {
        const fill = dot.querySelector(".carousel-dot-fill",);
        if (!fill) continue;
        fill.style.animation = "none";
        void fill.offsetWidth;
        fill.style.animation = "";
      }
      setActiveDot();
      timer = setTimeout(() => {
        goTo(getIndex() + 1,);
      }, DURATION,);
    };

    const setActiveDot = () => {
      const index = getIndex();
      for (const [dotIndex, dot] of dots.entries()) {
        const active = dotIndex === index;
        dot.classList.toggle("is-active", active,);
        dot.setAttribute("aria-current", active ? "true" : "false",);
        const fill = dot.querySelector(".carousel-dot-fill",);
        if (fill) {
          fill.style.animationDuration = `${DURATION}ms`;
        }
      }
    };

    prev?.addEventListener("click", () => { goTo(getIndex() - 1,); restartTimer(); },);
    next?.addEventListener("click", () => { goTo(getIndex() + 1,); restartTimer(); },);

    for (const dot of dots) {
      dot.addEventListener("click", () => {
        const index = Number(dot.getAttribute("data-index",) ?? "0",);
        goTo(index,);
        restartTimer();
      },);
    }

    track?.addEventListener("scroll", () => {
      if (ticking) {
        return;
      }
      ticking = true;
      window.requestAnimationFrame(() => {
        setActiveDot();
        ticking = false;
      },);
    },);

    setActiveDot();
    restartTimer();
    window.addEventListener("resize", setActiveDot,);
  }

  const whyDemo = document.querySelector("[data-why-demo]",);
  if (whyDemo) {
    const scenes = Array.from(whyDemo.querySelectorAll("[data-why-scene]",),);
    const dots = Array.from(whyDemo.querySelectorAll("[data-why-dot]",),);
    const fills = dots.map((dot,) => dot.querySelector("[data-why-fill]",),);
    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)",);
    const DURATION = 4000;
    let activeIndex = 0;
    let start = 0;
    let frame = 0;

    const render = (progress,) => {
      for (const [index, scene] of scenes.entries()) {
        scene.classList.toggle("is-active", index === activeIndex,);
      }

      for (const [index, dot] of dots.entries()) {
        dot.classList.toggle("is-active", index === activeIndex,);
        const fill = fills[index];
        if (!fill) continue;

        const scale =
          index < activeIndex
            ? 1
            : index === activeIndex
              ? progress
              : 0;

        fill.style.transform = `scaleX(${scale})`;
      }
    };

    const tick = (now,) => {
      const elapsed = now - start;
      if (elapsed >= DURATION) {
        activeIndex = (activeIndex + 1) % scenes.length;
        start = now;
        render(0,);
      } else {
        render(elapsed / DURATION,);
      }

      frame = window.requestAnimationFrame(tick,);
    };

    const stop = () => {
      if (frame) {
        window.cancelAnimationFrame(frame,);
        frame = 0;
      }
    };

    const startLoop = () => {
      stop();

      if (reducedMotion.matches || scenes.length === 0) {
        activeIndex = 0;
        render(1,);
        return;
      }

      start = performance.now();
      render(0,);
      frame = window.requestAnimationFrame(tick,);
    };

    startLoop();
    reducedMotion.addEventListener("change", startLoop,);
    document.addEventListener("visibilitychange", () => {
      if (document.hidden) {
        stop();
        return;
      }

      start = performance.now();
      frame = window.requestAnimationFrame(tick,);
    },);
  }
</script>

<style>
  :root {
    --bg: #f4f4f1;
    --bg-soft: #f8f8f6;
    --line: #e2e2db;
    --text: #252525;
    --muted: #646460;
    --accent: #5f70d8;
    --fs-xs: 11px;
    --fs-sm: 12px;
    --fs-md: 13px;
    --fs-lg: 14px;
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    overflow-x: hidden;
    font-family: "IBM Plex Mono", "SFMono-Regular", Menlo, monospace;
    font-weight: 400;
    font-size: var(--fs-md);
    line-height: 1.6;
    background: linear-gradient(150deg, #f6f6f4 0%, #efefeb 100%);
    color: var(--text);
    position: relative;
  }

  .ambient {
    position: fixed;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(circle at 5% 8%, #ffffff 0%, transparent 32%),
      radial-gradient(circle at 92% 92%, #e8ebff 0%, transparent 38%);
    opacity: 0.8;
  }

  .page-layout {
    position: relative;
    width: min(100%, 1160px);
    margin: 0 auto;
    padding: 0 24px;
    min-height: 100vh;
    display: flex;
    align-items: center;
    gap: 34px;
  }

  .shell {
    position: relative;
    width: 100%;
    max-width: 720px;
    flex: 1 1 auto;
    height: 100vh;
    overflow-y: auto;
    padding: 68px 0 88px;
    scrollbar-gutter: stable;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .shell::-webkit-scrollbar {
    width: 0;
    height: 0;
    display: none;
  }

  .brand-row {
    margin-bottom: 22px;
  }

  .brand {
    margin: 0;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: 0.01em;
  }
  .hero {
    position: relative;
    z-index: 1;
  }

  .hero h1 {
    margin: 0;
    font-size: var(--fs-lg);
    font-weight: 500;
    line-height: 1.6;
    letter-spacing: 0.01em;
  }

  .lede {
    margin-top: 18px;
    max-width: 56ch;
    font-size: var(--fs-md);
    line-height: 1.6;
    color: #3f3f3d;
  }

  .hero-note {
    margin: 12px 0 0;
    max-width: 56ch;
    color: var(--muted);
    font-size: var(--fs-sm);
  }

  .download-split {
    position: relative;
    display: inline-flex;
    margin-top: 14px;
  }

  .download-button {
    color: var(--accent);
    font-family: inherit;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    text-decoration: none;
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    padding: 8px 10px;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .brand-beta {
    border: 1px solid color-mix(in srgb, var(--accent) 30%, var(--line));
    background: color-mix(in srgb, var(--accent) 12%, #ffffff);
    color: var(--accent);
    padding: 2px 5px;
    font-size: var(--fs-xs);
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    line-height: 1;
    vertical-align: middle;
    position: relative;
    top: -1px;
  }

  .download-button:hover {
    border-color: color-mix(in srgb, var(--accent) 44%, var(--line));
    background: color-mix(in srgb, var(--accent) 14%, #ffffff);
  }

  .download-chevron {
    font-family: inherit;
    font-size: var(--fs-sm);
    color: var(--accent);
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    border-left: 0;
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    padding: 0 8px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
  }

  .download-chevron:hover {
    border-color: color-mix(in srgb, var(--accent) 44%, var(--line));
    background: color-mix(in srgb, var(--accent) 14%, #ffffff);
  }

  .download-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    margin-top: 4px;
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    background: #ffffff;
    z-index: 10;
  }

  .download-menu.is-open {
    display: flex;
    flex-direction: column;
  }

  .download-menu a,
  .download-menu a:visited {
    padding: 8px 10px;
    color: var(--text);
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    text-decoration: none;
  }

  .download-menu a:hover {
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    color: var(--accent);
  }

  .download-menu a.is-current {
    display: none;
  }

  .video-mobile {
    display: none;
    margin-top: 18px;
  }

  .panel {
    margin-top: 46px;
    border-top: 1px solid var(--line);
    padding-top: 24px;
  }

  h2 {
    margin: 0;
    color: #3f3f3d;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .detail-list,
  .link-list {
    list-style: none;
    padding: 0;
    margin: 16px 0 0;
  }

  .detail-list {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .why-layout {
    margin-top: 16px;
    display: flex;
    flex-direction: column;
    gap: 22px;
  }

  .why-demo {
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-soft) 90%, #ffffff);
  }

  .why-demo-header {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    padding: 10px 12px;
    border-bottom: 1px solid var(--line);
    color: #676762;
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .why-demo-screen {
    position: relative;
    min-height: 320px;
    padding: 16px;
    overflow: hidden;
    background:
      radial-gradient(circle at top left, color-mix(in srgb, #ffffff 88%, var(--accent)) 0%, transparent 34%),
      linear-gradient(180deg, color-mix(in srgb, var(--bg-soft) 68%, #ffffff) 0%, #f3f3ef 100%);
  }

  .why-scene {
    position: absolute;
    inset: 16px;
    opacity: 0;
    transform: translateY(8px) scale(0.985);
    transition:
      opacity 220ms ease,
      transform 220ms ease;
  }

  .why-scene.is-active {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .why-scene-label,
  .why-scene-title,
  .why-scene-detail {
    position: relative;
    z-index: 1;
  }

  .why-scene-label {
    margin: 0;
    color: var(--accent);
    font-size: var(--fs-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .why-scene-title {
    margin: 8px 0 0;
    max-width: 34ch;
    font-size: var(--fs-lg);
    font-weight: 500;
    line-height: 1.6;
  }

  .why-scene-detail {
    margin: 6px 0 0;
    max-width: 44ch;
    color: var(--muted);
    font-size: var(--fs-sm);
  }

  .scene-shell {
    margin-top: 18px;
    border: 1px solid color-mix(in srgb, var(--line) 84%, #c7cad5);
    background: rgba(255, 255, 255, 0.72);
    min-height: 168px;
    padding: 14px;
    display: grid;
    align-items: stretch;
    gap: 12px;
    box-shadow: 0 18px 40px rgba(37, 37, 37, 0.06);
  }

  .scene-timeline {
    grid-template-columns: 96px minmax(0, 1fr);
  }

  .scene-sidebar,
  .scene-tree {
    display: flex;
    flex-direction: column;
    gap: 8px;
    color: #72726d;
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .scene-sidebar .is-current,
  .scene-tree .is-current {
    color: var(--accent);
    font-weight: 500;
  }

  .scene-notes {
    display: grid;
    gap: 8px;
  }

  .scene-note-card,
  .scene-editor,
  .scene-repo-card,
  .scene-column {
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.92);
    padding: 10px;
  }

  .scene-note-card p,
  .scene-editor p,
  .scene-column p,
  .scene-repo-card p {
    margin: 0 0 8px;
    color: #454540;
    font-size: var(--fs-sm);
    font-weight: 500;
  }

  .scene-note-card span,
  .scene-editor span,
  .scene-tree span,
  .scene-sidebar span,
  .scene-column span,
  .scene-stat-row span,
  .scene-recurring-row span {
    display: block;
  }

  .scene-note-card span,
  .scene-editor span,
  .scene-column span,
  .scene-stat-row span {
    color: #5f5f5a;
    font-size: var(--fs-sm);
  }

  .scene-note-card.is-current {
    border-color: color-mix(in srgb, var(--accent) 28%, var(--line));
    transform: translateX(-10px);
  }

  .scene-note-card.is-current .is-accent {
    color: var(--accent);
  }

  .scene-rollover {
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
  }

  .scene-transfer,
  .scene-repeat-badge {
    color: var(--accent);
    font-size: var(--fs-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-align: center;
  }

  .scene-column {
    display: grid;
    gap: 8px;
    min-height: 136px;
    align-content: start;
  }

  .scene-column.is-target {
    border-color: color-mix(in srgb, var(--accent) 28%, var(--line));
  }

  .task-chip {
    border: 1px solid color-mix(in srgb, var(--line) 78%, #d2d5de);
    background: #f6f6f3;
    padding: 6px 8px;
  }

  .task-chip.is-done {
    color: #8a8a84;
    text-decoration: line-through;
  }

  .task-chip.is-highlight {
    border-color: color-mix(in srgb, var(--accent) 34%, var(--line));
    background: color-mix(in srgb, var(--accent) 9%, #ffffff);
  }

  .scene-recurring {
    position: relative;
    gap: 10px;
  }

  .scene-recurring-row {
    display: grid;
    grid-template-columns: 76px minmax(0, 1fr);
    align-items: center;
    gap: 10px;
  }

  .scene-markdown {
    grid-template-columns: 108px minmax(0, 1fr);
  }

  .scene-editor {
    display: grid;
    gap: 8px;
  }

  .scene-open-source {
    align-content: center;
  }

  .scene-repo-card {
    display: grid;
    gap: 10px;
    min-height: 168px;
  }

  .scene-stat-row {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
  }

  .scene-stat-row span {
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d4d7de);
    background: #f8f8f5;
    padding: 10px;
  }

  .why-scene-dots {
    position: absolute;
    left: 16px;
    right: 16px;
    bottom: 14px;
    display: flex;
    gap: 6px;
  }

  .why-scene-dot {
    flex: 1 1 0;
    height: 4px;
    background: rgba(95, 95, 90, 0.12);
    overflow: hidden;
  }

  .why-scene-dot-fill {
    display: block;
    width: 100%;
    height: 100%;
    background: color-mix(in srgb, var(--accent) 90%, #7c8ae8);
    transform-origin: left;
    transform: scaleX(0);
  }

  .detail-item {
    display: block;
  }

  .item-title {
    margin: 0;
    font-size: var(--fs-lg);
    font-weight: 500;
  }

  .link-list span {
    color: var(--accent);
  }

  .item-copy {
    margin: 6px 0 0;
    font-size: var(--fs-md);
    font-weight: 400;
    color: var(--muted);
    line-height: 1.6;
  }

  .link-list li {
    padding: 9px 0;
    border-bottom: 1px dashed #e9e9e2;
    font-size: var(--fs-md);
    font-weight: 400;
  }

  .link-list a {
    color: var(--text);
    text-decoration: none;
    display: flex;
    justify-content: space-between;
    gap: 16px;
  }

  .link-list a:hover {
    color: color-mix(in srgb, var(--text) 82%, var(--accent));
  }

  .carousel {
    margin-top: 16px;
  }

  .carousel-controls {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    margin-bottom: 10px;
  }

  .carousel-button {
    border: 1px solid var(--line);
    background: var(--bg-soft);
    color: #60605c;
    width: 30px;
    height: 24px;
    line-height: 1;
    font: inherit;
    font-size: var(--fs-md);
    cursor: pointer;
  }

  .carousel-button:hover {
    color: var(--accent);
    border-color: color-mix(in srgb, var(--line) 65%, var(--accent));
  }

  .carousel-track {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
  }

  .carousel-track::-webkit-scrollbar {
    display: none;
  }

  .example-card {
    min-width: 100%;
    scroll-snap-align: start;
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-soft) 84%, #ffffff);
    padding: 14px;
  }

  .example-title {
    margin: 0;
    font-size: var(--fs-lg);
    font-weight: 500;
  }

  .example-summary {
    margin: 6px 0 0;
    color: #555550;
    font-size: var(--fs-md);
  }

  .example-preview {
    margin-top: 10px;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #c2c4ce);
    background: color-mix(in srgb, #ffffff 60%, var(--bg-soft));
    padding: 10px;
    min-height: 138px;
    display: grid;
    align-content: center;
    justify-items: center;
    gap: 8px;
  }

  .example-image-placeholder {
    margin: 0;
    color: #5d5d58;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .example-image-path {
    margin: 0;
    color: #6c6c67;
    font-size: var(--fs-xs);
  }

  .carousel-dots {
    display: flex;
    justify-content: center;
    gap: 6px;
    margin-top: 10px;
  }

  .carousel-dot {
    width: 16px;
    height: 5px;
    border: 0;
    padding: 0;
    background: #d5d5ce;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .carousel-dot-fill {
    position: absolute;
    inset: 0;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: left;
  }

  .carousel-dot.is-active .carousel-dot-fill {
    animation: dot-fill 5000ms linear forwards;
  }

  @keyframes dot-fill {
    to {
      transform: scaleX(1);
    }
  }

  .video-column {
    position: relative;
    flex: 0 0 320px;
    width: 320px;
    padding-top: 0;
  }

  .video-frame {
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-soft) 90%, #ffffff);
    padding: 8px;
  }

  .product-video {
    display: block;
    width: 100%;
    max-width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #bfc2ce);
    background: #efefe9;
  }

  .video-column .product-video {
    aspect-ratio: 9 / 16;
  }

  .video-note {
    margin: 10px 0 0;
    color: #676762;
    font-size: var(--fs-xs);
  }

  footer {
    margin-top: 42px;
    border-top: 1px solid var(--line);
    padding-top: 24px;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
  }

  nav a {
    color: var(--accent);
    text-decoration: none;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  nav a:hover {
    text-decoration: underline;
  }

  .enter {
    opacity: 0;
    transform: translateY(14px);
    animation: rise 560ms cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    animation-delay: var(--delay);
  }

  @keyframes rise {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (min-width: 981px) {
    body {
      overflow: hidden;
    }
  }

  @media (max-width: 980px) {
    .page-layout {
      display: block;
      min-height: 0;
      padding: 42px 18px 62px;
    }

    .shell {
      max-width: none;
      height: auto;
      overflow: visible;
      padding: 0;
      scrollbar-gutter: auto;
    }

    .video-column {
      display: none;
    }

    .video-mobile {
      display: block;
    }

    .video-mobile .product-video {
      aspect-ratio: auto;
    }
  }

  @media (max-width: 720px) {
    .shell {
      width: 100%;
    }

    .brand {
      font-size: 15px;
    }

    .hero h1 {
      font-size: var(--fs-lg);
    }

    .link-list li {
      font-size: var(--fs-md);
    }

    .why-demo-header,
    .scene-rollover,
    .scene-markdown,
    .scene-timeline,
    .scene-stat-row {
      grid-template-columns: 1fr;
    }

    .why-demo-header {
      display: grid;
    }

    .scene-transfer {
      padding: 2px 0;
    }

    .scene-sidebar,
    .scene-tree {
      gap: 4px;
    }

    .why-demo-screen {
      min-height: 420px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .enter,
    .carousel-dot.is-active .carousel-dot-fill {
      animation: none;
      transition: none;
    }
  }
</style>
