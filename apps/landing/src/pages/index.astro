---
import { getCollection, } from "astro:content";

const capabilities = [
  {
    title: "Daily notes in one timeline",
    description: "Review older notes in a continuous feed instead of bouncing through tabs and old files.",
    visual: "timeline",
  },
  {
    title: "Unfinished tasks roll forward",
    description: "If a task is still open when the date changes, Philo brings it into today automatically.",
    visual: "rollover",
  },
  {
    title: "Recurring work comes back on its own",
    description: "Daily and scheduled tasks can return automatically, so you do not have to rebuild the same plan every morning.",
    visual: "recurring",
  },
  {
    title: "Markdown, not lock-in",
    description: "Keep your journal as plain markdown on disk and keep using tools like Obsidian or Logseq if you want.",
    visual: "markdown",
  },
  {
    title: "Free and open source",
    description: "Philo is meant to augment your vault, not turn your daily notes into another subscription silo.",
    visual: "open-source",
  },
];

const examples = [
  {
    title: "Habit tracker",
    summary: "Track streaks and daily completion without leaving your note.",
    imagePath: "/examples/habit-tracker.png",
  },
  {
    title: "World clock",
    summary: "See your team time zones while planning tomorrow.",
    imagePath: "/examples/world-clock.png",
  },
  {
    title: "Calorie counter",
    summary: "Quick meal log with totals inline in the journal.",
    imagePath: "/examples/calorie-counter.png",
  },
  {
    title: "Countdown board",
    summary: "Keep launches and deadlines visible at a glance.",
    imagePath: "/examples/countdown-board.png",
  },
  {
    title: "Mood chart",
    summary: "Simple emotional trendline over the week.",
    imagePath: "/examples/mood-chart.png",
  },
  {
    title: "Meeting prep checklist",
    summary: "Turn rough notes into ready talking points.",
    imagePath: "/examples/meeting-prep-checklist.png",
  },
];

const posts = await getCollection("blog");
posts.sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Philo | Daily notes that carry unfinished work forward</title>
    <meta
      name="description"
      content="Open source, local-first daily notes app that keeps markdown in your Obsidian vault and rolls unfinished tasks into today automatically."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=Instrument+Serif:ital@0;1&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="ambient"></div>
    <div class="page-layout">
      <main class="shell">
        <header class="enter" style="--delay: 0ms;">
          <div class="brand-row">
            <p class="brand">Philo <span class="brand-beta">beta</span></p>
          </div>
        </header>

        <section class="hero enter" style="--delay: 70ms;">
          <h1>Daily notes that carry unfinished work forward.</h1>
          <p class="lede">
            Philo started with one problem: daily notes are a great place to think, but unfinished
            tasks get buried in yesterday&apos;s file. It keeps your journal as markdown in your
            Obsidian vault, shows notes in a timeline, and rolls open tasks into today
            automatically.
          </p>
          <p class="hero-note">
            It is built first for people who already live in daily notes and want a calmer planning
            loop, not a bigger system to manage.
          </p>
          <div class="download-split" data-download>
            <a class="download-button" data-download-primary href="https://github.com/ComputelessComputer/philo/releases/latest">
              Download for macOS
            </a><button type="button" class="download-chevron" data-download-toggle aria-label="Other platforms"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg></button>
            <div class="download-menu" data-download-menu>
              <a data-download-macos href="https://github.com/ComputelessComputer/philo/releases/latest">macOS</a>
              <a data-download-windows href="https://github.com/ComputelessComputer/philo/releases/latest">Windows</a>
              <a data-download-linux href="https://github.com/ComputelessComputer/philo/releases/latest">Linux</a>
            </div>
          </div>
          <div class="hero-media-mobile">
            <div class="video-frame">
              <img
                class="hero-screenshot"
                src="/philo-hero-screenshot.svg"
                alt="Philo showing daily notes in a timeline with unfinished tasks carried into today"
              />
            </div>
          </div>
        </section>

        <section class="panel enter" style="--delay: 140ms;">
          <h2>Why Philo exists</h2>
          <ul class="detail-list">
            {
              capabilities.map((item,) => (
                <li class="detail-item">
                  <div class="detail-copy">
                    <p class="item-title">{item.title}</p>
                    <p class="item-copy">{item.description}</p>
                  </div>
                  <div class={`detail-visual detail-visual-${item.visual}`} aria-hidden="true">
                    {
                      item.visual === "timeline" && (
                        <div class="mini-timeline">
                          <div class="mini-timeline-track">
                            <div class="mini-timeline-day">
                              <p>Mar 13</p>
                              <span class="mini-skeleton mini-skeleton-short"></span>
                              <span class="mini-skeleton"></span>
                              <span class="mini-skeleton mini-skeleton-fade"></span>
                            </div>
                            <div class="mini-timeline-day">
                              <p>Mar 12</p>
                              <span class="mini-skeleton mini-skeleton-short"></span>
                              <span class="mini-skeleton"></span>
                              <span class="mini-skeleton mini-skeleton-fade"></span>
                            </div>
                            <div class="mini-timeline-day">
                              <p>Mar 11</p>
                              <span class="mini-skeleton mini-skeleton-short"></span>
                              <span class="mini-skeleton"></span>
                              <span class="mini-skeleton mini-skeleton-fade"></span>
                            </div>
                            <div class="mini-timeline-day">
                              <p>Mar 10</p>
                              <span class="mini-skeleton mini-skeleton-short"></span>
                              <span class="mini-skeleton"></span>
                              <span class="mini-skeleton mini-skeleton-fade"></span>
                            </div>
                            <div class="mini-timeline-day">
                              <p>Mar 9</p>
                              <span class="mini-skeleton mini-skeleton-short"></span>
                              <span class="mini-skeleton"></span>
                              <span class="mini-skeleton mini-skeleton-fade"></span>
                            </div>
                          </div>
                        </div>
                      )
                    }
                    {
                      item.visual === "rollover" && (
                        <div class="mini-rollover">
                          <div class="mini-rollover-columns">
                            <div class="mini-rollover-column mini-rollover-source">
                              <span class="mini-today-pill mini-today-pill-source">Today</span>
                              <p>Mar 16</p>
                              <div class="mini-task-row">
                                <span class="mini-checkbox is-checked"></span>
                                <span class="mini-task-bar mini-task-bar-wide"></span>
                              </div>
                              <div class="mini-task-row mini-task-row-fading">
                                <span class="mini-checkbox"></span>
                                <span class="mini-task-bar"></span>
                              </div>
                              <div class="mini-task-row mini-task-row-fading mini-task-row-fading-delay">
                                <span class="mini-checkbox"></span>
                                <span class="mini-task-bar mini-task-bar-short"></span>
                              </div>
                            </div>
                            <div class="mini-rollover-column mini-rollover-target">
                              <span class="mini-today-pill mini-today-pill-target">Today</span>
                              <p>Mar 17</p>
                              <div class="mini-task-row">
                                <span class="mini-checkbox"></span>
                                <span class="mini-task-bar"></span>
                              </div>
                              <div class="mini-task-row">
                                <span class="mini-checkbox"></span>
                                <span class="mini-task-bar mini-task-bar-short"></span>
                              </div>
                            </div>
                          </div>
                          <span class="mini-moving-task mini-moving-task-first"></span>
                          <span class="mini-moving-task mini-moving-task-second"></span>
                        </div>
                      )
                    }
                    {
                      item.visual === "recurring" && (
                        <div class="mini-recurring">
                          <div class="mini-recurring-task">
                            <span class="mini-checkbox"></span>
                            <span class="mini-task-bar mini-task-bar-wide"></span>
                            <span class="mini-frequency-chip">Daily</span>
                          </div>
                        </div>
                      )
                    }
                    {
                      item.visual === "markdown" && (
                        <div class="mini-markdown-apps">
                          <div class="mini-app-icon mini-app-icon-obsidian">
                            <svg viewBox="0 0 32 32" aria-hidden="true">
                              <path d="M17 3 8 9v12l8 8 8-8V9Z" fill="#6F6AF8"></path>
                              <path d="M17 3 13 14l3 15 8-8V9Z" fill="#5146E6"></path>
                            </svg>
                          </div>
                          <div class="mini-app-icon mini-app-icon-logseq">
                            <svg viewBox="0 0 32 32" aria-hidden="true">
                              <circle cx="16" cy="7" r="4" fill="#EAB308"></circle>
                              <circle cx="9" cy="18" r="4" fill="#10B981"></circle>
                              <circle cx="23" cy="18" r="4" fill="#3B82F6"></circle>
                              <circle cx="16" cy="26" r="4" fill="#F97316"></circle>
                            </svg>
                          </div>
                          <div class="mini-app-icon mini-app-icon-markdown">
                            <svg viewBox="0 0 32 32" aria-hidden="true">
                              <rect x="3" y="6" width="26" height="20" rx="4" fill="none" stroke="currentColor" stroke-width="2"></rect>
                              <path d="M8 21v-9l4 5 4-5v9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                              <path d="m22 12 3 4h-2v5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                          </div>
                        </div>
                      )
                    }
                    {
                      item.visual === "open-source" && (
                        <div class="mini-open-source">
                          <div class="mini-github-icon">
                            <svg viewBox="0 0 24 24" aria-hidden="true">
                              <path fill="currentColor" d="M12 .5a12 12 0 0 0-3.79 23.39c.6.11.82-.26.82-.58v-2.03c-3.34.72-4.04-1.41-4.04-1.41-.55-1.38-1.33-1.75-1.33-1.75-1.09-.73.08-.71.08-.71 1.2.09 1.84 1.24 1.84 1.24 1.08 1.83 2.82 1.3 3.5.99.11-.78.42-1.3.76-1.59-2.67-.3-5.47-1.33-5.47-5.92 0-1.31.47-2.38 1.24-3.22-.13-.3-.54-1.52.12-3.18 0 0 1.01-.32 3.3 1.23a11.4 11.4 0 0 1 6 0c2.29-1.55 3.29-1.23 3.29-1.23.67 1.66.26 2.88.13 3.18.77.84 1.23 1.91 1.23 3.22 0 4.6-2.81 5.61-5.49 5.91.43.38.81 1.1.81 2.23v3.3c0 .32.21.7.82.58A12 12 0 0 0 12 .5Z"></path>
                            </svg>
                          </div>
                          <div class="mini-stars">
                            <span class="mini-stars-label">GitHub</span>
                            <span class="mini-stars-value"><span data-github-stars>20</span> stars</span>
                          </div>
                        </div>
                      )
                    }
                  </div>
                </li>
              ))
            }
          </ul>
        </section>

        <section class="panel enter" style="--delay: 210ms;">
          <h2>Extra utilities, if you want them</h2>
          <p class="item-copy">
            The core product is still the daily note. Widgets, AI help, and computer-use workflows
            are add-ons for the moments when plain text is not enough.
          </p>
          <div class="carousel" data-carousel>
            <div class="carousel-controls">
              <button type="button" class="carousel-button" data-carousel-prev aria-label="Previous example">←</button>
              <button type="button" class="carousel-button" data-carousel-next aria-label="Next example">→</button>
            </div>
            <div class="carousel-track" data-carousel-track>
              {
                examples.map((item,) => (
                  <article class="example-card">
                    <p class="example-title">{item.title}</p>
                    <p class="example-summary">{item.summary}</p>
                    <div class="example-preview" role="img" aria-label={`${item.title} image placeholder`}>
                      <p class="example-image-placeholder">Add image later</p>
                      <p class="example-image-path">{item.imagePath}</p>
                    </div>
                  </article>
                ))
              }
            </div>
            <div class="carousel-dots">
              {
                examples.map((item, index,) => (
                  <button
                    type="button"
                    class={`carousel-dot ${index === 0 ? "is-active" : ""}`}
                    data-carousel-dot
                    data-index={index}
                    aria-label={`Go to ${item.title}`}
                    aria-current={index === 0 ? "true" : "false"}
                  ><span class="carousel-dot-fill"></span></button>
                ))
              }
            </div>
          </div>
        </section>

        <section class="panel enter" style="--delay: 280ms;">
          <h2>Notes from the team</h2>
          <ul class="link-list">
            {
              posts.map((post,) => (
                <li>
                  <a href={`/blog/${post.slug}`}>
                    {post.data.title}
                    <span>→</span>
                  </a>
                </li>
              ))
            }
          </ul>
        </section>

        <section class="panel enter" style="--delay: 350ms;">
          <h2>Built to sit beside your vault, not replace it</h2>
          <p class="item-copy">
            Philo is open source and free forever. Your notes stay as markdown files on disk, and
            the app is meant to make daily notes lighter, not trap them in a new format or workflow.
          </p>
        </section>

        <footer class="enter" style="--delay: 420ms;">
          <nav>
            <a href="https://github.com/ComputelessComputer/philo">GitHub</a>
            <a href="/blog">Blog</a>
            <a href="/privacy">Privacy</a>
            <a href="/terms">Terms</a>
            <a href="mailto:john@hyprnote.com">Email</a>
          </nav>
        </footer>
      </main>

      <aside class="video-column enter" style="--delay: 120ms;">
        <div class="video-frame">
          <div class="product-demo">
            <input
              class="product-demo-input"
              type="radio"
              name="product-demo"
              id="product-demo-capture"
              checked
            />
            <input
              class="product-demo-input"
              type="radio"
              name="product-demo"
              id="product-demo-rollover"
            />
            <input
              class="product-demo-input"
              type="radio"
              name="product-demo"
              id="product-demo-recurring"
            />

            <div class="product-demo-tabs" role="tablist" aria-label="Philo demo views">
              <label for="product-demo-capture">Capture</label>
              <label for="product-demo-rollover">Rollover</label>
              <label for="product-demo-recurring">Recurring</label>
            </div>

            <div class="product-demo-window">
              <div class="product-demo-titlebar" aria-hidden="true">
                <div class="product-demo-dots">
                  <span></span>
                  <span></span>
                  <span></span>
                </div>
                <svg class="product-demo-pin" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path
                    d="M13.8 3.2 20.8 10.2l-2.4.8-3.5 3.5.6 5.8-1 .9-3.8-5-5 3.8-.9-1 .6-5.8 3.5-3.5.8-2.4Z"
                    fill="currentColor"
                  ></path>
                </svg>
              </div>

              <div class="product-demo-panel product-demo-panel-capture">
                <div class="product-demo-note">
                  <div class="product-demo-header">
                    <p class="product-demo-date">March 17th</p>
                    <span class="product-demo-pill product-demo-pill-today">Today</span>
                    <span class="product-demo-city">Seoul</span>
                  </div>

                  <div class="product-demo-body">
                    <p>couldn&apos;t agree more</p>
                    <a href="https://www.youtube.com/watch?v=ZJEnQ0sMt-sU">https://www.youtube.com/watch?v=ZJEnQ0sMt-sU</a>

                    <p>might be a good idea for marketing website</p>
                    <a href="https://x.com/RomaTesla/status/2033528717155373390?s=20">https://x.com/RomaTesla/status/2033528717155373390?s=20</a>

                    <p>nice tool <a href="https://dither.neato.fun">https://dither.neato.fun</a></p>

                    <p>
                      distribution &amp; development. after coming back from america, keep the work
                      moving without rebuilding the list from scratch.
                    </p>
                  </div>

                  <ul class="product-demo-tasks" aria-label="Example Philo tasks">
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">study remotion skills</span>
                      <span class="product-demo-chip product-demo-chip-today">Today</span>
                    </li>
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">export the Hilton reservation</span>
                      <span class="product-demo-chip product-demo-chip-today">Today</span>
                    </li>
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">
                        <strong>finish q2 planning</strong> to prepare for the next quarter
                      </span>
                      <span class="product-demo-chip product-demo-chip-date">Mar 20</span>
                    </li>
                    <li class="product-demo-subtask">
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">
                        create the document and wire up a deterministic report script
                      </span>
                    </li>
                  </ul>
                </div>
              </div>

              <div class="product-demo-panel product-demo-panel-rollover">
                <div class="product-demo-note">
                  <div class="product-demo-header">
                    <p class="product-demo-date">March 17th</p>
                    <span class="product-demo-pill product-demo-pill-today">Today</span>
                    <span class="product-demo-city">Seoul</span>
                  </div>

                  <div class="product-demo-status">
                    <span>2 open tasks rolled in from yesterday</span>
                  </div>

                  <div class="product-demo-rollover-cards" aria-hidden="true">
                    <div class="product-demo-rollover-card product-demo-rollover-card-old">
                      <p>Mar 16</p>
                      <span></span>
                      <span></span>
                    </div>
                    <div class="product-demo-rollover-arrow"></div>
                    <div class="product-demo-rollover-card product-demo-rollover-card-new">
                      <p>Mar 17</p>
                      <span></span>
                      <span></span>
                    </div>
                  </div>

                  <ul class="product-demo-tasks" aria-label="Rolled over Philo tasks">
                    <li class="product-demo-task-highlight">
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">send revised landing copy to design</span>
                      <span class="product-demo-chip product-demo-chip-origin">From Mar 16</span>
                    </li>
                    <li class="product-demo-task-highlight product-demo-task-highlight-delay">
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">capture the release notes changes</span>
                      <span class="product-demo-chip product-demo-chip-origin">From Mar 16</span>
                    </li>
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">review tonight&apos;s recurring tasks</span>
                      <span class="product-demo-chip product-demo-chip-today">Today</span>
                    </li>
                  </ul>
                </div>
              </div>

              <div class="product-demo-panel product-demo-panel-recurring">
                <div class="product-demo-note">
                  <div class="product-demo-header">
                    <p class="product-demo-date">March 17th</p>
                    <span class="product-demo-pill product-demo-pill-today">Today</span>
                    <span class="product-demo-city">Seoul</span>
                  </div>

                  <div class="product-demo-body">
                    <p>daily note, but without the friction of rebuilding the same checklist.</p>
                  </div>

                  <ul class="product-demo-tasks" aria-label="Recurring Philo tasks">
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">close the day with one clear next step</span>
                      <span class="product-demo-chip product-demo-chip-recurring">Daily</span>
                    </li>
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">check launch blockers before noon</span>
                      <span class="product-demo-chip product-demo-chip-recurring">Weekdays</span>
                    </li>
                    <li>
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">pull unfinished tasks back into focus</span>
                      <span class="product-demo-chip product-demo-chip-today">Today</span>
                    </li>
                    <li class="product-demo-subtask">
                      <span class="product-demo-checkbox"></span>
                      <span class="product-demo-task-copy">leave the rest inside markdown</span>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
        <p class="video-note">Click through a live mockup of how Philo captures notes, rolls tasks, and keeps recurring work visible.</p>
      </aside>
    </div>
  </body>
</html>

<script is:inline>
  (async () => {
    const REPO = "ComputelessComputer/philo";
    const FALLBACK = `https://github.com/${REPO}/releases/latest`;
    const primary = document.querySelector("[data-download-primary]",);
    const toggle = document.querySelector("[data-download-toggle]",);
    const menu = document.querySelector("[data-download-menu]",);
    const macLink = document.querySelector("[data-download-macos]",);
    const winLink = document.querySelector("[data-download-windows]",);
    const linuxLink = document.querySelector("[data-download-linux]",);
    const starEls = Array.from(document.querySelectorAll("[data-github-stars]",),);

    toggle?.addEventListener("click", (e,) => {
      e.stopPropagation();
      menu?.classList.toggle("is-open",);
    },);

    document.addEventListener("click", () => {
      menu?.classList.remove("is-open",);
    },);

    try {
      const res = await fetch(`https://api.github.com/repos/${REPO}/releases/latest`,);
      if (!res.ok) throw new Error();
      const data = await res.json();
      const assets = data.assets ?? [];

      const find = (/** @type {RegExp} */ re,) => assets.find((a,) => re.test(a.name,),)?.browser_download_url;

      const urls = {
        macos: find(/\.dmg$/,) ?? FALLBACK,
        windows: find(/\.exe$/,) ?? FALLBACK,
        linux: find(/\.AppImage$/,) ?? FALLBACK,
      };

      if (macLink) macLink.href = urls.macos;
      if (winLink) winLink.href = urls.windows;
      if (linuxLink) linuxLink.href = urls.linux;

      const ua = navigator.userAgent.toLowerCase();
      let os = "macos";
      if (ua.includes("win",)) os = "windows";
      else if (ua.includes("linux",)) os = "linux";

      const labels = { macos: "Download for macOS", windows: "Download for Windows", linux: "Download for Linux" };
      if (primary) {
        primary.href = urls[os];
primary.firstChild.textContent = labels[os];
      }

      const currentLink = { macos: macLink, windows: winLink, linux: linuxLink }[os];
      if (currentLink) currentLink.classList.add("is-current",);
    } catch {
      // keep fallback links
    }

    try {
      const repoRes = await fetch(`https://api.github.com/repos/${REPO}`,);
      if (!repoRes.ok) throw new Error();
      const repoData = await repoRes.json();
      const stars = new Intl.NumberFormat().format(repoData.stargazers_count ?? 0,);
      for (const el of starEls) {
        el.textContent = stars;
      }
    } catch {
      // keep fallback star count
    }
  })();

  const carousel = document.querySelector("[data-carousel]",);
  if (carousel) {
    const track = carousel.querySelector("[data-carousel-track]",);
    const prev = carousel.querySelector("[data-carousel-prev]",);
    const next = carousel.querySelector("[data-carousel-next]",);
    const dots = Array.from(carousel.querySelectorAll("[data-carousel-dot]",),);
    const total = track?.children.length ?? 0;
    const DURATION = 5000;
    let ticking = false;
    let timer = null;

    const getIndex = () => {
      if (!track) {
        return 0;
      }
      return Math.round(track.scrollLeft / track.clientWidth,);
    };

    const goTo = (index,) => {
      if (!track || total === 0) {
        return;
      }
      const nextIndex = (index + total) % total;
      track.scrollTo({
        left: nextIndex * track.clientWidth,
        behavior: "smooth",
      },);
    };

    const restartTimer = () => {
      clearTimeout(timer,);
      for (const dot of dots) {
        const fill = dot.querySelector(".carousel-dot-fill",);
        if (!fill) continue;
        fill.style.animation = "none";
        void fill.offsetWidth;
        fill.style.animation = "";
      }
      setActiveDot();
      timer = setTimeout(() => {
        goTo(getIndex() + 1,);
      }, DURATION,);
    };

    const setActiveDot = () => {
      const index = getIndex();
      for (const [dotIndex, dot] of dots.entries()) {
        const active = dotIndex === index;
        dot.classList.toggle("is-active", active,);
        dot.setAttribute("aria-current", active ? "true" : "false",);
        const fill = dot.querySelector(".carousel-dot-fill",);
        if (fill) {
          fill.style.animationDuration = `${DURATION}ms`;
        }
      }
    };

    prev?.addEventListener("click", () => { goTo(getIndex() - 1,); restartTimer(); },);
    next?.addEventListener("click", () => { goTo(getIndex() + 1,); restartTimer(); },);

    for (const dot of dots) {
      dot.addEventListener("click", () => {
        const index = Number(dot.getAttribute("data-index",) ?? "0",);
        goTo(index,);
        restartTimer();
      },);
    }

    track?.addEventListener("scroll", () => {
      if (ticking) {
        return;
      }
      ticking = true;
      window.requestAnimationFrame(() => {
        setActiveDot();
        ticking = false;
      },);
    },);

    setActiveDot();
    restartTimer();
    window.addEventListener("resize", setActiveDot,);
  }

</script>

<style>
  :root {
    --bg: #f4f4f1;
    --bg-soft: #f8f8f6;
    --line: #e2e2db;
    --text: #252525;
    --muted: #646460;
    --accent: #5f70d8;
    --fs-xs: 11px;
    --fs-sm: 12px;
    --fs-md: 13px;
    --fs-lg: 14px;
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    overflow-x: hidden;
    font-family: "IBM Plex Mono", "SFMono-Regular", Menlo, monospace;
    font-weight: 400;
    font-size: var(--fs-md);
    line-height: 1.6;
    background: linear-gradient(150deg, #f6f6f4 0%, #efefeb 100%);
    color: var(--text);
    position: relative;
  }

  .ambient {
    position: fixed;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(circle at 5% 8%, #ffffff 0%, transparent 32%),
      radial-gradient(circle at 92% 92%, #e8ebff 0%, transparent 38%);
    opacity: 0.8;
  }

  .page-layout {
    position: relative;
    width: min(100%, 1240px);
    margin: 0 auto;
    padding: 0 24px;
    min-height: 100vh;
    display: flex;
    align-items: flex-start;
    gap: 42px;
  }

  .shell {
    position: relative;
    width: 100%;
    max-width: 780px;
    flex: 1 1 auto;
    padding: 68px 0 88px;
  }

  .brand-row {
    margin-bottom: 22px;
  }

  .brand {
    margin: 0;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: 0.01em;
  }
  .hero {
    position: relative;
    z-index: 1;
  }

  .hero h1 {
    margin: 0;
    font-size: 18px;
    font-weight: 500;
    line-height: 1.45;
    letter-spacing: 0.01em;
  }

  .lede {
    margin-top: 18px;
    max-width: 72ch;
    font-size: 15px;
    line-height: 1.72;
    color: #3f3f3d;
  }

  .hero-note {
    margin: 16px 0 0;
    max-width: 72ch;
    color: var(--muted);
    font-size: 13px;
    line-height: 1.72;
  }

  .download-split {
    position: relative;
    display: inline-flex;
    margin-top: 14px;
  }

  .download-button {
    color: var(--accent);
    font-family: inherit;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    text-decoration: none;
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    padding: 8px 10px;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .brand-beta {
    border: 1px solid color-mix(in srgb, var(--accent) 30%, var(--line));
    background: color-mix(in srgb, var(--accent) 12%, #ffffff);
    color: var(--accent);
    padding: 2px 5px;
    font-size: var(--fs-xs);
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    line-height: 1;
    vertical-align: middle;
    position: relative;
    top: -1px;
  }

  .download-button:hover {
    border-color: color-mix(in srgb, var(--accent) 44%, var(--line));
    background: color-mix(in srgb, var(--accent) 14%, #ffffff);
  }

  .download-chevron {
    font-family: inherit;
    font-size: var(--fs-sm);
    color: var(--accent);
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    border-left: 0;
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    padding: 0 8px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
  }

  .download-chevron:hover {
    border-color: color-mix(in srgb, var(--accent) 44%, var(--line));
    background: color-mix(in srgb, var(--accent) 14%, #ffffff);
  }

  .download-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    margin-top: 4px;
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    background: #ffffff;
    z-index: 10;
  }

  .download-menu.is-open {
    display: flex;
    flex-direction: column;
  }

  .download-menu a,
  .download-menu a:visited {
    padding: 8px 10px;
    color: var(--text);
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    text-decoration: none;
  }

  .download-menu a:hover {
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    color: var(--accent);
  }

  .download-menu a.is-current {
    display: none;
  }

  .hero-media-mobile {
    display: none;
    margin-top: 18px;
  }

  .panel {
    margin-top: 46px;
    border-top: 1px solid var(--line);
    padding-top: 24px;
  }

  h2 {
    margin: 0;
    color: #3f3f3d;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .detail-list,
  .link-list {
    list-style: none;
    padding: 0;
    margin: 16px 0 0;
  }

  .detail-list {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .detail-item {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 220px;
    gap: 16px;
    align-items: start;
  }

  .detail-visual {
    inline-size: 220px;
    block-size: 78px;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #bfc2ce);
    background:
      radial-gradient(circle at top left, rgba(255, 255, 255, 0.9) 0%, transparent 42%),
      color-mix(in srgb, var(--bg-soft) 90%, #ffffff);
    padding: 8px;
    overflow: hidden;
    display: grid;
    align-items: center;
  }

  .mini-timeline,
  .mini-rollover,
  .mini-recurring,
  .mini-markdown-apps,
  .mini-open-source {
    width: 100%;
    height: 100%;
  }

  .mini-timeline {
    --mini-timeline-day-height: 44px;
    --mini-timeline-gap: 6px;
    --mini-timeline-step: calc(var(--mini-timeline-day-height) + var(--mini-timeline-gap));
    position: relative;
    overflow: hidden;
  }

  .mini-timeline-track {
    display: grid;
    gap: var(--mini-timeline-gap);
    animation: mini-timeline-scroll 6s ease-in-out infinite;
  }

  .mini-timeline-day,
  .mini-rollover-column {
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.9);
    padding: 4px;
    display: grid;
    gap: 4px;
    position: relative;
  }

  .mini-timeline-day {
    block-size: var(--mini-timeline-day-height);
    align-content: start;
  }

  .mini-timeline-day p,
  .mini-rollover-column p,
  .mini-stars-label,
  .mini-stars-value {
    margin: 0;
  }

  .mini-timeline-day p,
  .mini-rollover-column p {
    color: #454540;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .mini-skeleton {
    display: block;
    height: 5px;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d1d4de);
    background: rgba(95, 95, 90, 0.08);
  }

  .mini-skeleton-short {
    width: 56%;
  }

  .mini-skeleton-fade {
    display: none;
  }

  .mini-rollover {
    position: relative;
  }

  .mini-rollover-columns {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 6px;
  }

  .mini-rollover-source {
    animation: mini-rollover-source-focus 5s ease-in-out infinite;
  }

  .mini-rollover-target {
    border-color: color-mix(in srgb, var(--accent) 28%, var(--line));
    animation: mini-rollover-target-focus 5s ease-in-out infinite;
  }

  .mini-task-row,
  .mini-recurring-task {
    display: grid;
    grid-template-columns: 8px minmax(0, 1fr);
    gap: 5px;
    align-items: center;
  }

  .mini-task-row-fading {
    animation: mini-source-task-fade 5s ease-in-out infinite;
  }

  .mini-task-row-fading-delay {
    animation-delay: 0.25s;
  }

  .mini-checkbox {
    display: block;
    width: 8px;
    height: 8px;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #bfc2ce);
    background: #ffffff;
    position: relative;
  }

  .mini-checkbox.is-checked {
    background: color-mix(in srgb, var(--accent) 16%, #ffffff);
    border-color: color-mix(in srgb, var(--accent) 34%, var(--line));
  }

  .mini-checkbox.is-checked::after {
    content: "";
    position: absolute;
    left: 1px;
    top: -1px;
    width: 3px;
    height: 6px;
    border-right: 1.5px solid var(--accent);
    border-bottom: 1.5px solid var(--accent);
    transform: rotate(40deg);
  }

  .mini-task-bar {
    display: block;
    height: 6px;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d1d4de);
    background: #f6f6f3;
  }

  .mini-task-bar-wide {
    width: 86%;
  }

  .mini-task-bar-short {
    width: 62%;
  }

  .mini-today-pill {
    position: absolute;
    top: 4px;
    right: 4px;
    padding: 0 4px;
    border: 1px solid color-mix(in srgb, var(--accent) 32%, var(--line));
    background: color-mix(in srgb, var(--accent) 10%, #ffffff);
    color: var(--accent);
    font-size: 8px;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .mini-today-pill-source {
    animation: mini-today-source-fade 5s ease-in-out infinite;
  }

  .mini-today-pill-target {
    opacity: 0;
    animation: mini-today-target-fade 5s ease-in-out infinite;
  }

  .mini-moving-task {
    position: absolute;
    left: 40%;
    width: 34px;
    height: 6px;
    border: 1px solid color-mix(in srgb, var(--accent) 34%, var(--line));
    background: color-mix(in srgb, var(--accent) 12%, #ffffff);
    opacity: 0;
  }

  .mini-moving-task-first {
    top: 28px;
    animation: mini-rollover-move 5s ease-in-out infinite;
  }

  .mini-moving-task-second {
    top: 44px;
    width: 26px;
    animation: mini-rollover-move 5s ease-in-out infinite 0.25s;
  }

  .mini-recurring {
    display: flex;
    align-items: center;
  }

  .mini-recurring-task {
    width: 100%;
    grid-template-columns: 8px minmax(0, 1fr) auto;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.9);
    padding: 6px;
  }

  .mini-frequency-chip {
    padding: 1px 5px;
    border: 1px solid color-mix(in srgb, var(--accent) 34%, var(--line));
    background: color-mix(in srgb, var(--accent) 10%, #ffffff);
    color: var(--accent);
    font-size: 8px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .mini-markdown-apps {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
  }

  .mini-app-icon {
    width: 30px;
    height: 30px;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.9);
    display: grid;
    place-items: center;
  }

  .mini-app-icon svg {
    width: 18px;
    height: 18px;
    display: block;
  }

  .mini-app-icon-markdown {
    color: #3f3f3d;
  }

  .mini-open-source {
    display: grid;
    grid-template-columns: 30px minmax(0, 1fr);
    gap: 8px;
    align-items: center;
    height: 100%;
  }

  .mini-github-icon {
    width: 30px;
    height: 30px;
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.92);
    color: #252525;
    display: grid;
    place-items: center;
  }

  .mini-github-icon svg {
    width: 16px;
    height: 16px;
  }

  .mini-stars {
    display: grid;
    gap: 4px;
  }

  .mini-stars-label {
    color: #676762;
    font-size: 8px;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .mini-stars-value {
    color: #3f3f3d;
    font-size: 11px;
    font-weight: 500;
  }

  @keyframes mini-timeline-scroll {
    0%,
    18% {
      transform: translateY(0);
    }

    28%,
    46% {
      transform: translateY(calc(-1 * var(--mini-timeline-step)));
    }

    56%,
    74% {
      transform: translateY(calc(-2 * var(--mini-timeline-step)));
    }

    84%,
    100% {
      transform: translateY(calc(-2 * var(--mini-timeline-step)));
    }
  }

  @keyframes mini-rollover-source-focus {
    0%,
    48% {
      box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 34%, var(--line));
    }

    55%,
    100% {
      box-shadow: inset 0 0 0 1px transparent;
    }
  }

  @keyframes mini-rollover-target-focus {
    0%,
    48% {
      box-shadow: inset 0 0 0 1px transparent;
    }

    55%,
    100% {
      box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 34%, var(--line));
    }
  }

  @keyframes mini-today-source-fade {
    0%,
    45% {
      opacity: 1;
    }

    55%,
    100% {
      opacity: 0;
    }
  }

  @keyframes mini-today-target-fade {
    0%,
    45% {
      opacity: 0;
    }

    55%,
    100% {
      opacity: 1;
    }
  }

  @keyframes mini-rollover-move {
    0%,
    38% {
      opacity: 0;
      transform: translateX(-40px);
    }

    45%,
    72% {
      opacity: 1;
      transform: translateX(10px);
    }

    80%,
    100% {
      opacity: 0;
      transform: translateX(34px);
    }
  }

  @keyframes mini-source-task-fade {
    0%,
    40% {
      opacity: 1;
    }

    55%,
    100% {
      opacity: 0.28;
    }
  }

  .item-title {
    margin: 0;
    font-size: var(--fs-lg);
    font-weight: 500;
  }

  .link-list span {
    color: var(--accent);
  }

  .item-copy {
    margin: 6px 0 0;
    font-size: var(--fs-md);
    font-weight: 400;
    color: var(--muted);
    line-height: 1.6;
  }

  .link-list li {
    padding: 9px 0;
    border-bottom: 1px dashed #e9e9e2;
    font-size: var(--fs-md);
    font-weight: 400;
  }

  .link-list a {
    color: var(--text);
    text-decoration: none;
    display: flex;
    justify-content: space-between;
    gap: 16px;
  }

  .link-list a:hover {
    color: color-mix(in srgb, var(--text) 82%, var(--accent));
  }

  .carousel {
    margin-top: 16px;
  }

  .carousel-controls {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    margin-bottom: 10px;
  }

  .carousel-button {
    border: 1px solid var(--line);
    background: var(--bg-soft);
    color: #60605c;
    width: 30px;
    height: 24px;
    line-height: 1;
    font: inherit;
    font-size: var(--fs-md);
    cursor: pointer;
  }

  .carousel-button:hover {
    color: var(--accent);
    border-color: color-mix(in srgb, var(--line) 65%, var(--accent));
  }

  .carousel-track {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
  }

  .carousel-track::-webkit-scrollbar {
    display: none;
  }

  .example-card {
    min-width: 100%;
    scroll-snap-align: start;
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-soft) 84%, #ffffff);
    padding: 14px;
  }

  .example-title {
    margin: 0;
    font-size: var(--fs-lg);
    font-weight: 500;
  }

  .example-summary {
    margin: 6px 0 0;
    color: #555550;
    font-size: var(--fs-md);
  }

  .example-preview {
    margin-top: 10px;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #c2c4ce);
    background: color-mix(in srgb, #ffffff 60%, var(--bg-soft));
    padding: 10px;
    min-height: 138px;
    display: grid;
    align-content: center;
    justify-items: center;
    gap: 8px;
  }

  .example-image-placeholder {
    margin: 0;
    color: #5d5d58;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .example-image-path {
    margin: 0;
    color: #6c6c67;
    font-size: var(--fs-xs);
  }

  .carousel-dots {
    display: flex;
    justify-content: center;
    gap: 6px;
    margin-top: 10px;
  }

  .carousel-dot {
    width: 16px;
    height: 5px;
    border: 0;
    padding: 0;
    background: #d5d5ce;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .carousel-dot-fill {
    position: absolute;
    inset: 0;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: left;
  }

  .carousel-dot.is-active .carousel-dot-fill {
    animation: dot-fill 5000ms linear forwards;
  }

  @keyframes dot-fill {
    to {
      transform: scaleX(1);
    }
  }

  .video-column {
    position: sticky;
    top: 68px;
    align-self: flex-start;
    flex: 0 0 320px;
    width: 320px;
    padding-top: 0;
  }

  .video-frame {
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-soft) 90%, #ffffff);
    padding: 8px;
  }

  .product-demo {
    position: relative;
  }

  .product-demo-input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .product-demo-tabs {
    display: flex;
    gap: 6px;
    margin-bottom: 8px;
  }

  .product-demo-tabs label {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 5px 9px;
    border: 1px solid color-mix(in srgb, var(--line) 84%, #c8cad4);
    background: rgba(255, 255, 255, 0.88);
    color: #66665f;
    font-size: var(--fs-xs);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    cursor: pointer;
    transition:
      border-color 160ms ease,
      background 160ms ease,
      color 160ms ease;
  }

  #product-demo-capture:checked ~ .product-demo-tabs label[for="product-demo-capture"],
  #product-demo-rollover:checked ~ .product-demo-tabs label[for="product-demo-rollover"],
  #product-demo-recurring:checked ~ .product-demo-tabs label[for="product-demo-recurring"] {
    border-color: color-mix(in srgb, var(--accent) 34%, var(--line));
    background: color-mix(in srgb, var(--accent) 10%, #ffffff);
    color: var(--accent);
  }

  .product-demo-window {
    position: relative;
    display: block;
    width: 100%;
    max-width: 100%;
    aspect-ratio: 9 / 16;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #bfc2ce);
    background:
      radial-gradient(circle at bottom right, rgba(201, 211, 255, 0.65), transparent 28%),
      linear-gradient(180deg, #f7f7f2 0%, #efeee7 100%);
    overflow: hidden;
  }

  .product-demo-titlebar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 16px 10px;
  }

  .product-demo-dots {
    display: flex;
    gap: 7px;
  }

  .product-demo-dots span {
    width: 11px;
    height: 11px;
    border-radius: 999px;
    border: 1px solid rgba(0, 0, 0, 0.08);
  }

  .product-demo-dots span:nth-child(1) {
    background: #ff6259;
  }

  .product-demo-dots span:nth-child(2) {
    background: #ffbd44;
  }

  .product-demo-dots span:nth-child(3) {
    background: #28c840;
  }

  .product-demo-pin {
    width: 16px;
    height: 16px;
    color: #a0a7b2;
  }

  .product-demo-panel {
    position: absolute;
    inset: 44px 14px 14px;
    opacity: 0;
    pointer-events: none;
    transform: translateY(8px);
    transition:
      opacity 180ms ease,
      transform 180ms ease;
  }

  #product-demo-capture:checked ~ .product-demo-window .product-demo-panel-capture,
  #product-demo-rollover:checked ~ .product-demo-window .product-demo-panel-rollover,
  #product-demo-recurring:checked ~ .product-demo-window .product-demo-panel-recurring {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0);
  }

  .product-demo-note {
    height: 100%;
    padding: 18px 18px 20px;
    background: rgba(255, 255, 255, 0.84);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.95) inset,
      0 24px 48px rgba(44, 49, 63, 0.08);
    backdrop-filter: blur(8px);
  }

  .product-demo-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 18px;
  }

  .product-demo-date {
    margin: 0;
    font-family: "Instrument Serif", Georgia, serif;
    font-size: 22px;
    font-style: italic;
    line-height: 1;
    letter-spacing: -0.02em;
    color: #1f2937;
  }

  .product-demo-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 4px 11px 3px;
    border-radius: 999px;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .product-demo-pill-today {
    background: linear-gradient(to bottom, #4b5563, #1f2937);
    color: #ffffff;
  }

  .product-demo-city {
    color: #9ca3af;
    font-size: 13px;
  }

  .product-demo-body {
    display: grid;
    gap: 8px;
    margin-bottom: 18px;
    color: #1f2937;
    font-size: 14px;
    line-height: 1.35;
  }

  .product-demo-body p {
    margin: 0;
  }

  .product-demo-body a {
    color: #3b82f6;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.12em;
    overflow-wrap: anywhere;
  }

  .product-demo-status {
    margin-bottom: 14px;
    padding: 9px 11px;
    border: 1px solid color-mix(in srgb, var(--accent) 26%, var(--line));
    background: color-mix(in srgb, var(--accent) 8%, #ffffff);
    color: #334155;
    font-size: 12px;
    font-weight: 500;
  }

  .product-demo-rollover-cards {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 20px minmax(0, 1fr);
    gap: 8px;
    align-items: center;
    margin-bottom: 16px;
  }

  .product-demo-rollover-card {
    border: 1px solid color-mix(in srgb, var(--line) 80%, #d3d6df);
    background: rgba(255, 255, 255, 0.82);
    padding: 8px;
    display: grid;
    gap: 6px;
  }

  .product-demo-rollover-card p {
    margin: 0;
    color: #4b5563;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .product-demo-rollover-card span {
    display: block;
    height: 7px;
    background: rgba(148, 163, 184, 0.18);
  }

  .product-demo-rollover-card-old {
    opacity: 0.68;
  }

  .product-demo-rollover-card-new {
    border-color: color-mix(in srgb, var(--accent) 34%, var(--line));
  }

  .product-demo-rollover-arrow {
    height: 2px;
    background: var(--accent);
    position: relative;
  }

  .product-demo-rollover-arrow::after {
    content: "";
    position: absolute;
    right: -1px;
    top: -4px;
    border-top: 5px solid transparent;
    border-bottom: 5px solid transparent;
    border-left: 7px solid var(--accent);
  }

  .product-demo-tasks {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 8px;
  }

  .product-demo-tasks li {
    display: grid;
    grid-template-columns: 14px minmax(0, 1fr) auto;
    gap: 8px;
    align-items: start;
    color: #1f2937;
    font-size: 14px;
    line-height: 1.3;
  }

  .product-demo-subtask {
    margin-left: 22px;
    grid-template-columns: 14px minmax(0, 1fr);
  }

  .product-demo-checkbox {
    width: 14px;
    height: 14px;
    border: 1.5px solid #1f2937;
    background: rgba(255, 255, 255, 0.92);
    box-sizing: border-box;
  }

  .product-demo-task-copy strong {
    font-weight: 600;
  }

  .product-demo-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 20px;
    padding: 0 10px;
    border-radius: 999px;
    border: 1px solid transparent;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .product-demo-chip-today {
    border-color: #fde68a;
    background: #fef3c7;
    color: #92400e;
  }

  .product-demo-chip-date {
    border-color: #bfdbfe;
    background: #dbeafe;
    color: #1d4ed8;
  }

  .product-demo-chip-origin {
    border-color: color-mix(in srgb, var(--accent) 28%, #c7d2fe);
    background: color-mix(in srgb, var(--accent) 10%, #ffffff);
    color: var(--accent);
  }

  .product-demo-chip-recurring {
    border-color: #fed7aa;
    background: #fff7ed;
    color: #c2410c;
  }

  .product-demo-task-highlight {
    animation: product-demo-task-highlight 3.8s ease-in-out infinite;
  }

  .product-demo-task-highlight-delay {
    animation-delay: 0.22s;
  }

  .hero-screenshot {
    display: block;
    width: 100%;
    max-width: 100%;
    height: auto;
    border: 1px dashed color-mix(in srgb, var(--line) 82%, #bfc2ce);
    background: #efefe9;
  }

  .video-note {
    margin: 10px 0 0;
    color: #676762;
    font-size: var(--fs-xs);
  }

  @keyframes product-demo-task-highlight {
    0%,
    100% {
      transform: translateX(0);
      opacity: 1;
    }

    35% {
      transform: translateX(2px);
      opacity: 1;
    }

    55% {
      transform: translateX(-1px);
      opacity: 0.86;
    }
  }

  footer {
    margin-top: 42px;
    border-top: 1px solid var(--line);
    padding-top: 24px;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
  }

  nav a {
    color: var(--accent);
    text-decoration: none;
    font-size: var(--fs-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  nav a:hover {
    text-decoration: underline;
  }

  .enter {
    opacity: 0;
    transform: translateY(14px);
    animation: rise 560ms cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    animation-delay: var(--delay);
  }

  @keyframes rise {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 980px) {
    .page-layout {
      display: block;
      min-height: 0;
      padding: 42px 18px 62px;
    }

    .shell {
      max-width: none;
      height: auto;
      overflow: visible;
      padding: 0;
      scrollbar-gutter: auto;
    }

    .video-column {
      display: none;
    }

    .hero-media-mobile {
      display: block;
    }
  }

  @media (max-width: 720px) {
    .shell {
      width: 100%;
    }

    .brand {
      font-size: 15px;
    }

    .hero h1 {
      font-size: 16px;
      line-height: 1.5;
    }

    .lede,
    .hero-note {
      max-width: none;
    }

    .link-list li {
      font-size: var(--fs-md);
    }

    .detail-item,
    .mini-transfer,
    .mini-files,
    .mini-repo-row {
      grid-template-columns: 1fr;
    }

    .detail-visual {
      inline-size: 220px;
    }

    .mini-arrow {
      width: 14px;
      margin: 0 auto;
      transform: rotate(90deg);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .enter,
    .carousel-dot.is-active .carousel-dot-fill {
      animation: none;
      transition: none;
    }
  }
</style>
